/**
 * TextField component barrel export.
 *
 * Provides a clean import path for the TextField component.
 * Allows importing as: import TextField from "../../components/TextField"
 * instead of: import TextField from "../../components/TextField/TextField"
 */
export { default } from "./TextField";
export type { TextFieldProps } from "./TextField";