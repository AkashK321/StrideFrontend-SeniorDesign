/**
 * Button component barrel export.
 *
 * Provides a clean import path for the Button component.
 * Allows importing as: import Button from "../../components/Button"
 * instead of: import Button from "../../components/Button/Button"
 */
export { default } from "./Button";

